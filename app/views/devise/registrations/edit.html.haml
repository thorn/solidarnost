%h2 Изменить информацию пользователя

= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put }, :html => {:class => "form-stacked"}) do |f| 
  = devise_error_messages!

  .field
    = f.label :email, "Почта"
    = f.email_field :email

  .field
    = f.label :password, "Пароль"
    %small
      (оставьте это поле пустым, если не хотите изменить пароль)
    %br
    = f.password_field :password

  .field
    = f.label :password_confirmation, "Подтверждение пароля"
    = f.password_field :password_confirmation

  = render 'info_fields', :f => f
  
  .field
    = f.label :current_password, "Текущий пароль"
    %small
      (введите текущий пароль, чтобы подтвердить изменения)
    %br
    = f.password_field :current_password

  .actions
    = f.submit "Сохранить", :class => "btn primary"
    = link_to "Back", :back, :class => "btn"

/ %h3 Удалить мою учетную запись

/ = link_to "Cancel my account", registration_path(resource_name), :confirm => "Are you sure?", :method => :delete
